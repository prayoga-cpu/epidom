# AGENTIC CHECKPOINT
# Minimal closeout record.

task: "fix french translation crashes on profile page"
gate: "ok"
success: "Added fallback to English for missing translations and return key if English also missing, preventing undefined `.replace` crashes on pages like profile account settings."

evidence:
  changed_files:
    - src/components/lang/i18n-provider.tsx
  checks:
    - pnpm type-check
    - pnpm lint
  rollback: "git checkout src/components/lang/i18n-provider.tsx"

risks:
  sensitive_without_escalation: false
  over_engineering: false
  claim_without_evidence: false

not_verified: []
next: "none"
