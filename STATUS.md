# STATUS.md

## Current State: v3 Dashboard Revamp — Phase 1 (POS Mode) + Phase 2 (Back Office) — ✅ CODE-COMPLETE + TEST-VERIFIED, ⚠️ NO LIVE/DEVICE PASS YET (branch `epidom-revamp`)

_(AI Agents: update the checklist below every time you finish a stage or a checklist item — check the box, don't re-describe finished work in prose. Keep this file scoped to the active phase; once both phases ship to `main`, fold a short summary into the changelog and reset this file for the next phase.)_

Specs: `docs/dashboard-revamp.md` (Phase 1), `docs/back-office-revamp.md` (Phase 2). Plan: `/Users/darwinprayoga/.claude/plans/stateful-juggling-seal.md`. Not deployed to production — isolated on `epidom-revamp`. **Stable preview URL (use this one, not a per-deploy hash link): https://epidom-git-epidom-revamp-prayogadevelopment-gmailcoms-projects.vercel.app** — always points at the latest deploy of this branch.

**Login on the preview URL was broken and is now fixed**: `NEXT_PUBLIC_APP_URL`/`BETTER_AUTH_URL`/`NEXTAUTH_URL` are project-wide Vercel env vars hardcoded to `https://epidom.fr` (production) with no prior per-branch override — `src/lib/auth.ts`'s `betterAuth({ baseURL: process.env.NEXT_PUBLIC_APP_URL })` uses that value to build every post-login redirect and OAuth callback URL, so any login attempt on the preview domain was completing then bouncing to production. Added a `gitBranch: "epidom-revamp"`-scoped override for all three, pointed at the stable preview URL above, via the Vercel API (`target: ["preview"]`, doesn't touch the shared production/other-branches value). **Caveat, not fixed**: if the login screen offers "Sign in with Google," that will likely still fail with a `redirect_uri_mismatch` — Google's OAuth client needs `https://epidom-git-epidom-revamp-prayogadevelopment-gmailcoms-projects.vercel.app/api/auth/callback/google` added to its authorized redirect URIs in Google Cloud Console, which is outside Vercel and this session has no access to fix. Email/password login should work correctly.

**Live testing on the preview found a real gap both phases' planning missed**: neither shell had a way to switch into the other. Phase 1's plan explicitly reasoned about POS→Back Office ("Cashier/Kitchen roles generally shouldn't be jumping into management pages") but never considered the reverse — an Owner/Manager legitimately needs a quick way from Back Office into the till. Added, all gated by role/plan the same way the rest of each shell already is:
- **Back Office → POS**: a prominent "switch to POS" CTA at the top of `sidebar.tsx`'s nav (both desktop rail and mobile drawer) — a filled button linking straight to `/store/{storeId}/pos` at POS tier or above, or a locked upgrade-styled variant below it, mirroring the existing locked-nav-item pattern. Hidden entirely for a staff persona without `/pos` access (defensive — in practice Cashier/Kitchen never reach Back Office at all).
- **POS Mode → Back Office**: a "Dashboard" link added to `pos-mode-overflow-menu.tsx`, visible only when the active persona's role is `OWNER` or `MANAGER` — Cashier/Kitchen don't see it, since their `allowedPages` has no Back Office page to land on.
- **`/stores` (before a store is even picked)**: each `StoreCard` now has a small "switch to POS" shortcut button (top-left corner, sibling to the existing edit/delete dropdown, not nested inside the card's own link) — shown when that business's shared subscription plan supports POS and the store isn't payment-blocked, linking directly to that specific store's `/pos`.

New tests: `sidebar.test.tsx` extended (switch-to-POS CTA plan-gating), `store-card.test.tsx` (new — 4 cases covering plan/blocked-state gating). Full suite: 117 files / 1292 tests passing.

**Three more fixes from the same live-testing round**:
1. **Denied-access fallback now lands on `/pos`, not `/dashboard`** (`require-staff-page-access.ts`) — the old fallback (`allowedPages[0] ?? "/dashboard"`) sent a denied staffer to `/dashboard`, a page most roles were never granted either; worse, an owner who'd unchecked every permission box on a staffer would infinite-loop them between two unreachable pages. Now prefers `/pos` unconditionally over array order (a customized `allowedPages` order shouldn't change where a denied user lands), falling back to their actual first allowed page only when `/pos` itself isn't granted (Kitchen → `/pos/kds`).
2. **Owner account labeled explicitly as the unrestricted "master" account** in Staff Management — the underlying behavior (role `OWNER` bypasses every `staffAllowedPages` check) already existed; added a one-line caption (`pages.staffOwnerMasterHint`, en/id/fr) making that explicit rather than implicit, so it's not mistaken for an ordinary staff row that happens to carry the `OWNER` role.
3. **POS Mode can now switch accounts / log out without detouring through Back Office's topbar.** That mechanism (switch to a different staffer, PIN-gated "back to Owner," full logout with cookie/service-worker-cache clearing) already existed, fully built, in `nav-user.tsx` — but was only reachable from Back Office's Topbar, which Cashier/Kitchen rarely if ever visit. Extracted the logic (untouched, same reload/clearing behavior) into `use-account-switcher.ts` so `nav-user.tsx` and the new `PosModeOverflowMenu` section share one implementation. Deliberately a separate section from Clock In/Out — clocking in/out is a timesheet action for the persona already active; switching is a different question ("who is this device speaking as").

New tests: `require-staff-page-access.test.ts` extended (3 new cases for the `/pos`-preferring fallback), `pos-mode-overflow-menu.test.tsx` (new — 7 cases covering the switcher section's role-based visibility). Full suite: 118 files / 1301 tests passing.

**Fourth request from the same round — date sort/filter on `/pos/schedule`'s "My History" list**: previously unscoped (server-side `take`-limited to the most recent 20 records, no date filter, no sort control). Added a `DateRangeField` (defaults to the last 30 days, matching the convention every other history/report view in the app already uses — `DateRangeField` has no "unset" state, so an unbounded default wasn't an option) plus a Newest/Oldest-first toggle (client-side reverse of the already-date-bounded, ≤50-row result set — no second query shape needed). Server side, `GET /stores/[id]/schedule/my-log` gained `from`/`to` query-param support, mirroring the manager-facing `/schedule/log` route's own parsing byte-for-byte — `fetchUnifiedLog` (the shared merge function both routes call) already supported date-range filtering, it just wasn't wired through this specific endpoint yet.

**Fifth request from the same round — "Back Office" resume CTA + "Back to Stores" + resumable POS shortcut**: three related live-testing asks, one underlying mechanism.
1. **POS Mode's "Dashboard" link restyled to a "Back Office" CTA** (`pos-mode-overflow-menu.tsx`) — was a plain outlined button; now a filled `bg-primary` (the same amber/gold used by `sidebar.tsx`'s reciprocal "switch to POS" CTA) button with a leading icon and trailing `ArrowRight`, visually matching that CTA exactly as requested (same yellow, same shape, same direction of travel).
2. **That button now resumes the last Back Office section actually visited in this store**, instead of always dropping onto `/dashboard`. New `LAST_VISITED_BACK_OFFICE_COOKIE` (`src/lib/last-visited.ts`) — separate from the existing app-wide `LAST_VISITED_COOKIE` because that one is legitimately overwritten by POS Mode pages too (a user currently sitting in POS Mode would just get bounced back into POS Mode if the "Back Office" button used the generic cookie). `LastVisitedTracker` now writes this second, narrower cookie/localStorage entry whenever the current path is a genuine Back Office (non-POS) page; `PosModeOverflowMenu` reads it client-side, validated against the current `storeId` so a stale value from a different store is never used. Same full-logout cleanup as the existing cookies (`use-account-switcher.ts`).
3. **The reciprocal mirror, POS→Back Office direction**: `/stores`' per-card POS shortcut (added in the third fix above) also now resumes the last POS screen (till, orders, KDS, or this store's own schedule view) instead of always opening the bare register — same mechanism, new `LAST_VISITED_POS_COOKIE` + `isPosAppPath()`.
4. **Added a "Back to Stores" action to POS Mode's account section** (`pos-mode-overflow-menu.tsx`) — same action, same `/stores` destination, same gating (`!actingAsStaff` — a staff PIN persona has no store list of its own) as the existing one in Back Office's `nav-user.tsx`, now reachable without leaving POS Mode first.
5. **Incidental fix surfaced while building this**: `RESUMABLE_STORE_SECTIONS` was missing `/pos/schedule` entirely — the "My Schedule" POS page existed as a real route but `isResumableAppPath`/the whole resume-on-sign-in mechanism didn't recognize it. Added.

New tests: `last-visited.test.ts` (new — 7 cases for `isBackOfficeAppPath`/`isPosAppPath`), `pos-mode-overflow-menu.test.tsx` extended (5 new cases — Back Office resume/fallback/cross-store rejection, Back to Stores visibility), `store-card.test.tsx` extended (2 new cases — POS resume/cross-store rejection). Full suite: 119 files / 1315 tests passing.

**Sixth request from the same round — device-picker labels, "go back" navigation, and a merged Role + Job-title picker with more templates**: a bundle of related live-testing asks.
1. **`StoreAccessGate`'s "Who's using this device?" staff cards now show each staffer's access (`Back Office + POS` / `Back Office` / `POS`) and role/job-title label**, not just a bare avatar+name. New `staffAccessLabelKey()` (`src/features/dashboard/shared/lib/staff-access-label.ts`) buckets a staff member's `allowedPages` against `posModeNavItems` — anything not in that set counts as Back Office. Reuses the existing `staffRoleLabel()` helper for the role line, so a `customRoleLabel` override displays correctly there too.
2. **"Go back" navigation added at two points the user identified as dead ends**: `StoreAccessGate`'s picker screen now has a "Back to Stores" button (previously the only way out was the PIN sub-view's staffer-deselect button, which doesn't leave the store at all); `/stores` now has an explicit back arrow to the marketing homepage (`SiteHeader`'s new `backHref` prop, wrapped with the logo in one flex group to preserve the header's `justify-content: space-between` layout with only 3 top-level children). Deliberately scoped to these two named pages, not an app-wide back-button audit — flagged to the user as the established pattern, not exhaustively applied elsewhere yet.
3. **Merged the Staff dialog's Role and Job-title selects into one dropdown, with "Use custom label" as a checkbox instead of a special dropdown value.** New `STAFF_ROLE_TEMPLATES` (`src/config/staff-permissions.config.ts`) replaces the old `CASHIER_JOB_TEMPLATES` — one flat list covering every role, including new back-office-only templates (**Admin**: full Back Office minus POS/till pages; **Finance**: narrow reporting-only — `/dashboard`, `/finance`, `/data` — both `StaffRole.MANAGER` underneath) alongside the front-of-house ones (**Waiter/Server**, **Bartender** — adds `/pos/kds` for bar-tagged orders, **Host** — Tables only, no register). All still `StaffRole.CASHIER`/`MANAGER`/`KITCHEN` underneath, not new enum values (would mean a Prisma migration + auditing every `staffRole === "..."` branch built this session). `PageAccessChecklist` gained a `templatePages`/`templateLabel` prop pair so its "customized?" detection and "Reset to role defaults" compare against the *selected template* (e.g. Bartender's page set), not just the bare role's — without this, picking Bartender would immediately show as "customized," and Reset would silently drop the KDS grant back to bare Cashier.
4. **Fixed a real misclassification bug surfaced while building #3's resume-cookie groundwork**: `/tables` is a POS Mode route (`src/app/(app)/store/[storeId]/(pos-mode)/tables/`) but doesn't start with `/pos` in its URL — `isBackOfficeAppPath`/`isPosAppPath` (added in the fifth request above) were classifying it as Back Office. Fixed with an explicit `POS_MODE_SECTIONS` set instead of a prefix check.
5. **Button loading-state visual glitch fixed globally**: the shared `Button` component's `transition-all` + `disabled:opacity-50` was producing a visible split-paint frame (half-repainted `oklch()` background) in WebKit when a Save button's `disabled` state toggled during an async save. Narrowed to `transition-colors` — no `Button` consumer in the app relies on `transition-all` for anything beyond color/opacity.

New tests: `staff-access-label.test.ts` (new), `page-access-checklist.test.tsx` extended (templatePages/templateLabel override behavior), `staff-permissions.config.test.ts` extended (STAFF_ROLE_TEMPLATES invariants — unique ids, back-office-only templates never grant a POS page and vice versa), `last-visited.test.ts` extended (`/tables` reclassification). Full suite: 121 files / 1333 tests passing.

**Seventh request from the same round — Owner/staff switching fixes, a locked-down Owner row, and store ownership transfer (v2.86.0)**: a chain of live-testing asks that started from one screenshot.
1. **Owner→Cashier switch left the Owner's page on screen.** `StoreAccessGate.verifyPin` only called `login()` (client state); `children` is a Server Component tree already rendered for the previous identity, so `requireOwnerOnly`/`requireStaffPageAccess` never re-ran. It now `window.location.reload()`s after a successful staff login — the same reasoning the old `handleSwitchedBackToOwner` already documented for the opposite direction.
2. **"Switch Account" unified and made non-destructive.** Removed the direct-to-Owner PIN shortcut (`handleBackToOwnerClick`/`handleSwitchedBackToOwner`, the two PIN dialogs in `nav-user.tsx` and `pos-mode-overflow-menu.tsx`, `useOwnerPinStatus` in `use-account-switcher.ts`). New transient, **unpersisted** `pickerOpen` on `usePosSession` (`partialize` excludes it) lets the picker show over a still-active session; `login()`/`logout()` reset it. `handleSwitchAccount` only opens it; `handleReturnToPicker` (clear + reload) stays for "Log Out of Staff Session". **Gotcha caught late:** the `(pos-mode)` layout nests `StoreAccessGate` *outside* `PosStaffGate`, so the outer gate always owns the picker in POS Mode — the cancel label is therefore chosen from `isPosAppPath(usePathname())` ("POS" vs "Back Office"), and the `PosStaffGate` changes I first made were unreachable and were reverted.
3. **Owner StaffMember row locked**: `PageAccessChecklist` gained a `master` prop (MASTER badge, every page checked + disabled, no handler at all); Contact Details, Pay Rate and the row's own PIN fields are hidden for `role === "OWNER"` (that row's `pin` is unrelated to the real Owner PIN on `Business.ownerPin` — OWNER rows are filtered out of the staff picker), replaced by pointers to Profile.
4. **Store ownership transfer** (`src/lib/services/store-transfer.service.ts`, routes under `/api/stores/[id]/transfer-ownership` and `/api/transfer-ownership/{lookup,accept}`, `/transfer-ownership/accept` page, `StoreOwnershipTransferCard` on the store-scoped Profile). Ownership is `Store.businessId`, so the transfer is one `businessId` reassignment in a transaction — but several things ride on the *Business* and had to be carried: `syncFinanceWithBusiness` stores get the old business's finance settings copied onto their own row first; a recipient with no Business gets one inheriting timezone/locale; the old owner's OWNER StaffMember row is scrubbed/deactivated (never deleted — orders reference it) and all `StaffSession`s for the store are deleted. The invite is a `Verification` row keyed by a 256-bit token (**no schema change, no migration**); token in POST bodies only, single-use via a `deleteMany` count check inside the transaction, sender's ownership re-verified inside it. Owner-only via `requireOwnerOnlyApi` + the default-deny staff policy. `safeInternalPath()` + `?next=` plumbing through login/register/verify-email so an invited new user lands back on the accept page; `/transfer-ownership` added to the proxy's public routes.
   - **Known limits, by decision:** plan store-limits are only enforced at store *creation*, so a transferred store is grandfathered onto the recipient's plan (feature gates still apply); a recipient's *existing* business keeps its own timezone, so the store adopts it; Stripe Connect follows the new owner. **Pre-existing, not touched:** `login-form.tsx` redirects to `?next=`/`?callbackUrl=` **unvalidated** (open redirect) — worth a follow-up.

New tests: `store-transfer.service.test.ts` (37), `store-transfer.test.ts` (44), `api/stores/transfer-ownership.test.ts` (24), `accept-transfer-client.test.tsx` (14), `store-ownership-transfer-card.test.tsx` (8), `use-pos-session.test.ts` (6), `store-access-gate.test.tsx` (11), `page-access-checklist.test.tsx` and `pos-mode-overflow-menu.test.tsx` extended. Mutation-checked: removing the in-transaction ownership re-check, or the `partialize` exclusion, each fails exactly the intended test. Full suite: 134 files / 1559 tests passing; `next build` succeeds. **Still not exercised in a real browser or against a real inbox** (no browser automation here, and `RESEND_API_KEY` is dev-mode) — the emailed link and sign-up-then-accept path need a human pass. Operator note: the link is built from `NEXT_PUBLIC_APP_URL` (see the App URL env triple).

**Eighth request from the same round — staff members get their own Epidom login (POS Mode only, v1)**: "on this email, has option to invite/connected … will have login to epidom and has this specific path to the store", plus an email-verified label in the staff drawer. Chosen with the user: a **real Better Auth account per staffer**, **PIN kept as a second factor on top**. Plan: `/Users/darwinprayoga/.claude/plans/stateful-juggling-seal.md`.
1. **Schema** (`20260918152719_add_staff_account_linking`, `20260918152956_add_staff_contract_details`, both additive, no backfill): `StaffMember.userId String? @unique` (FK→User, `onDelete: SetNull`) + `userLinkedAt`; new `StaffInvite` token table (32-byte CSPRNG, single-use `consumedAt`, 7-day TTL, email snapshotted at send time); `PayType.SALES`; `ContractType` + `StaffMember.contractType`. Applied to the Neon **development** branch only (confirmed via the Neon API first); they reach production only when this branch is merged and `pnpm build`'s `migrate deploy` runs there.
2. **The approved plan was under-scoped, and the safer design replaced it.** Widening 3–4 named routes to `"ownerOrStaff"` would not have worked or been safe: many guards encode "no PIN persona ⇒ owner" (a linked account is a real session that is *not* the owner, so it would have seen owner-only server-rendered data); most POS routes are plain handlers with inline ownership checks, not `requireStoreAuth`; `/api/subscriptions/status` would have auto-provisioned a FREE plan for the staffer and locked their POS; and `verify-pin` lets any caller assume any staffer, including a Manager with no PIN. Instead: **one central, default-deny policy table** (`src/lib/auth/staff-principal-policy.ts`: `METHOD /path/*` → phase pre/post-PIN, page grants, `managerOnly`, `selfRequired`, `nonManagerBodyKeys`) enforced from `withApiHandler` (every `requireStoreAuth` route) and `verifyStoreAccessWithResponse` (plain handlers). `verifyStoreAccess` = owner, else an ACTIVE non-OWNER linked `StaffMember`; `verifyStoreOwnership` is untouched. **Identity rule:** any `staffId`/`staffMemberId` a request names (query or JSON body) must be the caller's own linked member, and post-PIN routes need a live PIN persona for that same member — so the PIN really is a second factor. Guards/layouts/pages (`requireOwnerOnly`, `requireStaffPageAccess`, `requireManagerOrOwnerApi`, `requirePlan`, both store layouts, ~10 POS routes) are viewer-aware (`getStoreViewer`); plan is the store **owner's**.
3. **Claim flow** — public `/staff-invite?token=…` (`referrer: no-referrer`, noindex; in the proxy's public routes). No account for that email → choose a password, `auth.api.signUpEmail` creates it (the link click *is* the verification; `hasOnboarded: true`), client signs in, lands on `/go/pos`. Account exists → sign in with *that* account, then an explicit "Link this account". **Anti-takeover:** the emailed token can *create* an account for its exact address but never *link* an existing one — that requires the signed-in session's verified email to equal the invite's (case-insensitive); mismatch → 403 and a CRITICAL/DENIED audit event. Single use = atomic `updateMany({ consumedAt: null })` in the same transaction as the link; everything that can change between send and claim is re-checked at claim time (deactivated, made OWNER row, email edited, already linked, account appeared meanwhile, own store, linked elsewhere). Token only in POST bodies / query string (never a path the audit log records); IP rate limits; audit code `staff.invite.claim`.
4. **Owner side** (Staff dialog): "Sign-in account" block with Send/Resend invite; **the verified label is `hasLinkedAccount`** (derived from `userId`, no new status column — deliberately *not* `inviteStatus`, which flips to "accepted" the moment a PIN email is merely *sent*); mobile cards + desktop rows show Verified / Invite-sent badges. The invite route is owner-only, **awaits** delivery (502 and deletes the link if the mail provider refuses). PATCH/DELETE retire unclaimed invites when the email changes or the person is deactivated. Eligibility logic extracted to `staff-invite-ui.ts` (no email / unsaved edit / back-office-only role / deactivated all block sending, with the first obstacle named).
5. **What a linked staffer sees**: `/stores` lists their store (`accessRole: "staff"`, "Staff" badge, no edit/delete, opens their POS page directly, a "No POS access yet" state for a back-office-only role), no create-store button, and the gatekeeper no longer sends them to `/onboarding` (`GET /api/user/profile` now returns `staffLink`); `/api/subscriptions/status` no longer provisions a plan for a staff-only login; `/go/*` lands them on their store's POS page; the POS overflow menu hides Back Office and Switch Account for them and says "Log Out", not "Log Out of Owner Account". The roster `GET` returns only their own minimal row (never a coworker's contact details, pay, or PIN hash).
6. **Same round, unrelated to login**: Contact Details and Contract cards collapse when empty; "Pay rate" became **Contract** — contract type (Freelance / Part-time / Full-time / Contract) and pay basis (per hour / monthly / % of sales, capped at 100 server-side).
7. **Two real bugs found by auditing my own work before shipping it** (both fixed, mutation-checked): (a) the identity rule only looked at JSON bodies sent as `application/json`, but handlers call `request.json()` regardless — a `text/plain` `verify-pin` body naming another staffer bypassed the check entirely; it now parses the body whatever the header says. (b) The policy table was missing routes POS Mode actually calls: `GET finance/settings` (every store page's `CurrencyProvider` and the till's tax/service-charge pricing read it — denied, a staffer would have seen IDR and rung up wrong totals), `PATCH tables/*` (cycling a table's status shares a route with renaming it; non-managers may now send only `status`/`expectedStatus`), and `reservations` (the Tables page). Found by diffing every store API path the POS UI references against the table.
   - **Known limits, by decision (v1):** POS Mode only — Back Office redirects linked staff to their POS home (fail-closed), so back-office-only roles (Admin, Finance) can't usefully sign in yet and the invite UI blocks them; ~100 Back Office routes have no answer to "may a Cashier do this?". Still owner-only for staff accounts until added (one line each): **`cash-movements`** (money leaving the till — a test pins it as an owner decision), roster builder, supplier orders, push subscriptions. One linked store per Epidom user (`userId @unique`). A staffer with **no PIN set** signs in with the account alone (there's nothing to require), and there is no per-staffer PIN lockout — only the 30/min IP limit on `verify-pin`. Better Auth still sends its own "verify your email" mail on a new-account claim (redundant, harmless). A linked staffer who later becomes the owner via a store transfer stays linked; owner access wins everywhere (`verifyStoreAccess`, `/go`, `/stores` de-dupes).

New tests: `staff-invite.service.test.ts` (24 — incl. the anti-takeover refusal, single-use race, duplicate-account race), `staff-principal-policy.test.ts` (37, pinned allow-list), `store-guards-viewer.test.ts`, `require-plan.test.ts`, `staff-home.test.ts`, `staff-link.test.ts`, `store-verification.test.ts`, invite/lookup/complete/roster/PATCH/`/api/stores`/profile/status route tests, `/go` launcher (8), `staff-invite-claim-form.test.tsx` (19), `stores-container.test.tsx` (7), `store-card.test.tsx` and `pos-mode-overflow-menu.test.tsx` extended, `staff-invite-ui.test.ts`, `lib/staff-invite.test.ts`. Mutation-checked: dropping the content-type-agnostic body parse fails exactly the 5 bypass tests. Full suite: 150 files / 1743 tests passing; `tsc --noEmit` clean; `next build` succeeds. **HTTP-level end-to-end pass** against a local dev server and the Neon dev branch (throwaway rows, cleaned up; outbound email disabled): 44 checks covering a new-account claim, an existing-account link, the stranger-claims-someone-else's-invite attack (403, nothing written, CRITICAL/DENIED audit row), single-use replay, expired and email-edited links, real Better Auth sign-in, no subscription provisioned for a staff-only login, roster minimality, 403 before the PIN and 200 after it, coworker impersonation (JSON and `text/plain`), a dozen owner-only refusals, Back Office/`/staff` redirects, `/go/pos`, and the very next request being refused after deactivation — all behaved as designed. **Still not exercised in a real browser or against a real inbox** — the emailed link's rendering and click path, the claim page's UI, and a PIN login on a real tablet need a human pass (the link is built from `NEXT_PUBLIC_APP_URL`). **Operator note:** the nightly Neon reset had reverted the dev branch to prod's schema, wiping this feature's two (unmerged) migrations — re-applied with `prisma migrate deploy`; expect that again after each reset until the branch merges, and a preview build's `pnpm build` applies them itself.

**Orders page: POS/Online tabs, queue numbers, split view (2.88.0, 2026-09-19).** Prompted by photos of a competitor's counter POS.
- [x] POS | Online-ordering tabs with open-order counts replace the hidden "Source" add-filter (`pos-order-source-tabs.tsx`); a saved `sourceFilter: "ALL"` reads as POS (`toSourceTab`). Online = everything that isn't `source === "POS"`, same bucket as the card badge.
- [x] `Order.queueNumber` + `OrderQueueCounter` (migration `20260919120000_order_queue_number`): one `INSERT … ON CONFLICT DO UPDATE` per store per **store-local day** (`Business.timezone`, bad zone → UTC), taken inside the order-creating transaction just before the insert (`src/lib/services/order-queue-number.ts`). Wired into POS create, fresh hold and storefront checkout; **not** aggregator email imports, and finalize/merge/re-hold keep the number the row was born with. Split-checkout bills share the first bill's number (best-effort if committed concurrently).
- [x] Split view (new default, `pos-order-split-view.tsx`): status rail | 6-column list (order no., time, queue no., customer, table, total) | details panel with actions. Below `lg` the rail becomes chips and details open in a bottom `Sheet`. Grid/Compact/Board kept.
- [x] Order History: Queue and Table columns; queue number in the detail dialog title.
- [ ] **Not built, needs a decision:** History has no POS/Online tabs — its `source` param takes an exact `OrderSource` and ignores `"ONLINE"`, and an archive needs an "All" the two-tab spec doesn't have. The storefront order-status page and the cashier's order-complete dialog don't show the queue number yet (the customer/cashier are the people who actually need to read it out).
- [ ] **Operator:** restart any running `next dev` after `prisma generate` (the running process holds the old client). The nightly `reset_to_parent` of the Neon `development` branch drops this migration until it's redeployed — `npx prisma migrate deploy`. Production picks it up from the build's `prisma migrate deploy`.
- [ ] **Not verified in a browser or on an iPad:** three-column fit at 1024px, the bottom sheet, touch targets.

**Cashier revamp after Moka POS + Luna POS (2.89.0, 2026-09-19).** Prompted by screenshots of both. Plan: `/Users/darwinprayoga/.claude/plans/robust-doodling-whale.md`.
- [x] Live receipt-style cart, Order Queue button, Dine In/Take Away moved into the cart store (`pos-cart*.tsx`, `use-pos-cart.ts` — persist v1 migrates a v0 cart's manual discount); Save Bill (renamed Hold), Print Bill (`ReceiptData.documentType: "bill"`), Reprint, More sheet; Custom Item (`OrderItem.isCustom` + `department`, null = served immediately); Merge Bill (`pos/orders/merge`, HELD↔HELD only, sources CANCELLED); Split Bill (`pos-split-bill-dialog.tsx`, `allocateSplit`/`splitEqually`).
- [x] **Multi-tender:** an `OrderPayment` row for every new order; `PaymentMethod.SPLIT` only for ≥2 tenders. Every reader uses the rows and falls back to `Order.paymentMethod`/`total` for orders that predate them (no backfill): cash drawer (counts only CASH tenders), shift report, by-payment-method, payment-totals, filters, refunds (per-tender `refundedAmount`, `allocateRefund`), receipts. Zero-total bills and mark-paid of an unnamed PAY_LATER write no row by design.
- [x] Customers (`Customer`, `LoyaltyEntry`; spend/orders/last visit are computed, never cached), discount presets / coupons / loyalty points (`DiscountPreset`, `Coupon`, `StoreLoyaltySettings`; OPERATIONS-gated server-side via `storeHasPromotionsPlan`, offline degrades rather than rejects), Back Office Customers page + Data → Promotions tab, optional `Product.barcode` + scanner, send-receipt screen (email is new: `sendReceiptEmail`, capped at 10 sends/order), cash quick-buttons, Grid/Columns/List, one-row top bar (toolbar slot), Switch user.
- [x] Verified: `tsc` clean; 239 files / 3114 tests; a live dev-DB pass (37 checks, real handlers, only auth faked, incl. double-finalize / mark-paid / refund / cancel raced ×5 — 0 leftover rows); two independent adversarial reviews, which found and fixed 3 races, a refund race that skewed `expectedCash`, and a points under-reversal on repeat partial refunds.
- [ ] **Operator:** `npx prisma migrate deploy` for `20260919100000_pos_cashier_revamp` after each nightly `reset_to_parent` of the Neon `development` branch (already applied there once); restart any running `next dev` after `prisma generate`. Production applies it from the build's `prisma migrate deploy`. Additive except `order_receipt_sends.recipientPhone` → nullable.
- [ ] **Decision needed — pre-existing bug the live pass found, NOT fixed:** `PAYMENT_FEE_DEFAULTS` flat amounts (`STRIPE_CARD 2000`, `BANK_TRANSFER 4400`, `APPLE_PAY`/`GOOGLE_PAY 2000`, `src/config/payment-fees.config.ts`) are IDR-denominated but applied literally to every store, and `processingFeeEnabled` defaults to true with no overrides. A EUR store's 20.00 card sale records `processingFee = 2000.58`. Customers never see it, but net-margin / finance figures for non-IDR stores (France is the primary market) are wrong, and a split payment applies the flat once per tender. Suggested fix: key the defaults by `PaymentMarket`/currency (FRANCE and INTERNATIONAL flat 0 or EUR-scaled), seeded from `inferMarket()`; needs a call on whether to backfill historical orders.
- [ ] **Deliberately not built:** SMS receipts, camera barcode scanning, per-line tax/service flags on custom items, a take-away fee, merging non-saved bills, coupon/points with split-by-items, a partial-payment state, Customers CSV import.
- [ ] **Not verified in a browser or on an iPad:** touch layout of the cart panel / More sheet / checkout / complete screen, the one-row top bar at 768–1023px, a real barcode scanner, Bluetooth printing of bills and split receipts. The Xendit webhook's points credit, the shift-close route and the email/WhatsApp senders were unit-tested, not driven end to end.
- [ ] **Working tree:** this release and the 2.88.0 Orders-page work sit in one working tree — commit them separately (`queue-number.ts`, `order-queue-number.ts`, the `pos-order-*` split-view files and `20260919120000_order_queue_number` are 2.88.0's). This entry is numbered 2.89.0 because both first claimed 2.88.0 and `Release.version` is unique (the sync upserts by it, so a duplicate would have silently overwritten one release's notes).
- [ ] **AGENTS.md:** §6 i18n still says "do not add to fr.ts" although §2 and §7.2 reversed that on 2026-08-10 — worth fixing. French strings for the save/resume flow still fall back to English (those `pos.*` keys never existed in `fr.ts`).

**Shift page (2.90.0, 2026-09-19).** Prompted by photos of Moka POS's Shift, Finish Shift, end-shift confirmation and sales-summary screens.
- [x] `/pos/shift` (`(pos-mode)/pos/shift/page.tsx` → `features/pos/components/shift/`): open-shift card (opening cash), status card (who is on charge, start, opening cash, Finish shift, Cash in/out, live report), Finish Shift screen (cash rows built by `shift-summary.ts` from the server's `ShiftReportData` cash drawer, blind count, live difference, non-cash payments by window, order count, note), confirmation dialog, and an end-of-shift dialog (auto-print → preview → copy/open link). Till controls and the cash-movement dialog moved out of `my-schedule-list.tsx`.
- [x] Status-bar chip (`pos-mode-shift-chip.tsx`) + a "Shift" link in the More menu, both gated by `canManageShift` (`shift-access.ts`). Page access is the existing **"/pos"** grant — the same one the till APIs already require — so no new permission, no `allowedPages` migration, no `staff-principal-policy` change.
- [x] `useMyShift` also reconciles `usePosSession().shiftId`, which was only ever written at PIN login, so a shift opened (or closed) afterwards left later sales unattached (or attached to a closed till). Open/close write the response into the query cache before touching the session — two mutation-checked tests guard that race.
- [x] `ShiftReportDocument` extracted from `shift-report-print-view.tsx` so the in-app preview is the very document that prints. `usePrintShiftReport` mirrors `usePrintReceipt`; the automatic print only fires on an already-connected printer (`pairIfNeeded: false`) and never opens a device chooser.
- [x] Verified: `tsc` clean apart from one pre-existing unrelated error (`use-push-notifications.ts`); new tests for the summary maths, access rules, hooks, chip, overflow link, Finish screen, end dialog and page.
- [ ] **Not verified in a browser, on an iPad or on a real Bluetooth printer:** touch layout of the two-column Finish screen at 768–1024px, the end dialog's height with the report preview, the footer buttons on a 375px phone, the auto-print, and the AirPrint fallback. **Verify against live data:** open a shift on the dev DB, ring up a cash sale, confirm it appears in the expected cash, end the shift and compare the printed report.
- [ ] **Not built:** the closing note is saved on `Shift.notes` but is not on the printed or previewed report (the ESC/POS builder in `thermal-printer.ts` is being reworked in the working tree by another change — add a `notes` line to both renderers once that lands); the Back Office `/shifts` redirect still points at `/schedule`, which no longer holds till controls; existing staff need no change, but a Cashier whose owner customised `allowedPages` without `/pos` still has no till (as before).
- [ ] **Working tree:** this release sits beside unrelated uncommitted printer work (`printer-connection.ts`, `thermal-printer.ts`, `use-printer-settings.ts`, `receipt-labels.ts`, …) and storefront work — commit them separately. `shift-report-print-view.tsx` carries both a one-line printer edit and this release's document extraction.

**Multi-printer (2.92.0, 2026-09-19).** A till can drive a receipt (MAIN), kitchen, bar and label printer at once; asked for as "Cashier to customer / Captain order (Kitchen only, Bar only) / Label".
- [x] Transport: `lib/pwa/printer-connection.ts` — role → BLE link registry (two roles may share one physical printer, torn down only when the last lets go), a subscribe hook so the UI follows the hardware, and ONE global print queue (chunks of two jobs never interleave). `thermal-printer.ts` keeps its public surface and re-exports the connection functions, so `printReceipt` / `printShiftReport` (always MAIN) are unchanged for callers.
- [x] Builders: `order-ticket.ts` (no price, ever; big bold qty-first lines; sections per area) and `item-label.ts` (ESC/POS **and** TSPL, per-printer). `printer-test.ts` prints the real layout of each role for **Test print**.
- [x] Routing: `features/pos/lib/print-plan.ts` reuses the KDS's `itemDepartment()` — one rule, not two. Item → its area's printer; a missing area falls back to the other captain printer (one printer serves both); no captain printer → no ticket. A cart line carries no department, so `usePrintOrder` looks it up in the cached POS menu (cold cache → Kitchen, the KDS default).
- [x] Settings: `use-printer-settings.ts` is now per-role (persist v1; a v0 `{autoPrint, paperWidth}` becomes MAIN). `printer-settings-dialog.tsx` + a rewritten `pos-printer-menu.tsx`. Per-device (localStorage), no DB change.
- [x] Trigger: checkout success → `usePrintOrder(...).printOrder(input, { interactive: false })` — silent on success, a warning toast with **Connect & print** when a printer that should print is down; the complete screen has **Print order tickets** (REPRINT). HELD (Save Bill) orders never print: they reach the kitchen only at finalize, like the KDS.
- [x] `queueNumber` added to `PosOrderCreatedDto` / `buildPosOrderCreatedResponse` (additive).
- [x] fr/id/en strings (`pos.printers.*`); five `pos.print.*` keys fr.ts was missing (paper width, reprint, history) filled in because the rewritten popover shows them.
- [x] Independent adversarial review found five real defects, all fixed with regression tests: (1) `enabled` meant both "set up" and "prints now", so a print-on-demand bar printer sent drinks to the kitchen ticket (planner now routes on `enabled`, emits on `print`; the mutant that restores the bug fails 3 tests); (2) a connected-but-unusable device was never disconnected; (3) pairing now happens before any bytes are sent, so a long print can't burn the tap's user activation; (4) a per-write 10 s timeout drops a wedged printer instead of stalling the one shared queue for every role; (5) failed prints offer **Try again**.
- [ ] **Not verified on any real printer or tablet:** several concurrent BLE links on Android Chrome; the tall/bold ticket type on your models; **label printing on real hardware** (ESC/POS form-feed advancing to the next sticker; TSPL assumes 203 dpi, font "3", SIZE/GAP in mm); the 3-4 pairing taps after a reload; the settings dialog at 375px.
- [ ] **Not built:** auto-reconnect after a reload (`navigator.bluetooth.getDevices()` — would remove the per-shift re-pairing); LAN/Ethernet printers (a browser can't open a raw socket — needs a print bridge); auto-print for storefront / aggregator orders (no listener exists); reprinting tickets from Order History; a delta ticket when items are added to an already-placed order (Epidom has no add-to-placed-order flow today).
- [ ] **Working tree:** sits beside the Shift (2.90.0) and Google Reviews (2.91.0) work — commit the three separately. `shift-report-print-view.tsx` carries one line of this change (`s.printers.MAIN.paperWidth`) inside the Shift work's larger rewrite of that file.

**POS More menu: switch store, language, dark mode (2.93.0, 2026-09-19).** Asked for as "switch the store button / multilang toggle / dark and light mode toggle" on the POS More modal — the three controls the Back Office Topbar carries (`StoreSwitcher`, `LangSwitcher`, `ThemeToggle`) and POS Mode's shell doesn't.
- [x] `pos-mode-preferences.tsx` — FR/ID/EN pressed-button group (≥40px targets) + a dark-mode `Switch` whose whole `<label>` row is the tap target (the switch alone is ~18px). Deliberately not a reuse of `LangSwitcher`/`ThemeToggle`: both are styled for the navy Topbar, and `LangSwitcher`'s absolute dropdown would be clipped by the menu's scroll container. `LangSwitcher`'s option list is now exported as `LANGUAGE_OPTIONS` so the two can't drift. Shown to every persona (device preferences).
- [x] `pos-mode-store-switcher.tsx` — inline expanding list (no nested Popover/search box on top of a scrolling dialog). Opens the other store's **POS** (`/store/{id}/pos`, or a linked staff account's `staffHomePath`; disabled when that role has none), not the Topbar switcher's Back Office landing. Same visibility rule as "Back to Stores" (`hasAccountStoreList` in the menu): hidden for a staff PIN persona, kept for a linked staff account. Reads `GET /api/stores` only — no `staff-principal-policy.ts` line needed.
- [x] **Cart guard.** `epidom-pos-cart` (localStorage) carries no `storeId` and nothing resets it on a store change (only checkout, Clear sale, Save Bill and split-bill do), so a switch would carry the sale into the other store. The switcher confirms first (`useConfirm`) and clears only on confirm. Mutation-checked: dropping `clearCart()` fails 1 test, dropping the confirm fails 3. The Back Office `StoreSwitcher` and the /stores page have the same latent gap (switch store, then open POS) — not changed here.
- [x] fr/id/en: new `pos.switchStore.*`; `common.language.label` and `common.theme.dark` reused.
- [x] Verified: `tsc --noEmit` clean; `src/features/pos-mode` suite 75/75 (25 new: 8 preferences, 11 store switcher, 6 menu visibility rules).
- [ ] **Not verified in a browser or on an iPad:** the menu's height with the additions (it scrolls, `max-h-85dvh`) at 375px and on iPad; the FR/ID/EN toggle's fit on a ~343px dialog; a real store switch end to end (the persona picker at the destination store, plan gating on a store without POS).
- [ ] **Working tree:** `store-access-gate.tsx` and its test carry another change's uncommitted edits — not part of this release.

**Sequencing note**: `docs/dashboard-revamp.md` originally deferred Phase 2 until Phase 1 shipped with real usage data. The user explicitly chose to proceed with Phase 2 now anyway, fully aware there was no usage data yet and no existing wireframe brief for Back Office — Phase 2's first job was to *produce* that brief (`docs/back-office-revamp.md`), built with the same rigor as the original spec (real competitor research, real codebase audit), before implementing it.

**⚠️ Standing gap, both phases**: `tsc --noEmit` clean, `next build` succeeds locally and on Vercel, full `vitest run` passing (see each phase's own count below). Neither phase has been click-tested in a real browser or on a real device — this environment has no Playwright/browser automation available. Treat the unchecked items in each phase's own Stage 8/verification section as the real remaining work before merging to `main`.

### Summary table

| Phase | Stage | Status | Verified by |
|---|---|---|---|
| 1 | 0-7 (build) | ✅ Done | see Phase 1 detail below |
| 1 | 8 (verification) | ⚠️ Partial | `tsc`/`next build`/tests clean; real-device/browser pass outstanding |
| 2 | 1-4 (build) | ✅ Done | see Phase 2 detail below |
| 2 | 5-6 (audit + verification) | ⚠️ Partial | `tsc`/`next build`/tests clean; real-device/browser pass outstanding |

---

# Phase 1 — POS Mode

Split the dashboard into two shells — a bottom-tab-bar "POS Mode" for Cashier/Kitchen (iPad-first), and the existing left-rail "Back Office" for Owner/Manager. Confirmed decisions: upgrade-prompt CRO ships in both places (Back Office sidebar unchanged + a new POS Mode banner). Rollout is a clean one-shot route move, no runtime feature flag.

**A second verification pass caught 5 real regressions** the first pass's code-reading missed — writing and running actual tests found them within minutes. Removing `/pos`/`/pos/orders`/`/pos/kds`/`/tables` from `dashboardNavigation` (Stage 6) silently broke 5 other consumers that assumed that list meant "every page in the app" — including the PWA manifest's own installed `/go/pos` shortcut. All fixed, all now covered by a passing test.

## Stage 0 — Baseline

- [x] On `epidom-revamp`, working tree tracked.
- [x] Vercel preview verified green for this branch.
- [x] No DB migration needed (`Store.kitchenDisplayEnabled` already exists).

## Stage 1 — Route-group restructuring (one-shot move) — ✅ done

- [x] `(pos-mode)/layout.tsx` created — single `requirePlan(storeId, "POS")`, session/ownership checks mirrored from `(dashboard)/layout.tsx`, `StoreAccessGate` → `OfflineSyncProvider` → `PosStaffGate` → `PosModeShell` nesting.
- [x] `pos/page.tsx`, `pos/orders/page.tsx`, `pos/kds/page.tsx`, `tables/page.tsx` moved from `(dashboard)/` into `(pos-mode)/`, dropping the now-redundant page-header titles and the per-page `bypassStaffGate` logic (layout owns it now).
- [x] Old `(dashboard)/pos/`, `(dashboard)/tables/` directories deleted (including their `layout.tsx` `requirePlan` duplicates).
- [x] `pos-page-headers.tsx` deleted (fully unused after the move).
- [x] `(pos-mode)/loading.tsx` added — POS-Mode-shaped skeleton (item grid + cart), not the dashboard list skeleton.
- [x] `(pos-mode)/error.tsx` added — same stale-chunk recovery as `(dashboard)/error.tsx`, "back to safety" link points at `/pos` instead of `/dashboard`.
- [x] No hardcoded links assume `(dashboard)` in the path — confirmed via grep, `new-orders-card.tsx` and all others already use bare `/store/{id}/pos/...` URLs.
- [x] `src/proxy.ts` matcher confirmed to have no route-group-name dependency (untouched).
- [x] `/pos/display`, `/pos/orders/print`, `/pos/orders/daily-report` confirmed still resolving — `next build`'s route list shows all as distinct routes alongside `(pos-mode)`'s, zero collisions. **One real bug this caught and fixed**: the light schedule view was first placed at `(pos-mode)/schedule/page.tsx`, which resolved to `/schedule` and collided with Back Office's existing route — `next build` failed with an explicit parallel-pages error. Moved to `(pos-mode)/pos/schedule/page.tsx` → `/pos/schedule`, matching every other reference to it.

## Stage 2 — POS Mode shell chrome — ✅ done

- [x] `src/features/pos-mode/pos-mode-shell.tsx` — status bar → banner → `<main>` → tab bar, `calc(100dvh/var(--app-zoom,1))` per convention.
- [x] `pos-mode-status-bar.tsx` — store name (via `useCurrentStore()`)/online badge/staff badge (ported from `pos-header.tsx`'s desktop branch) + printer menu.
- [x] `pos-mode-tab-bar.tsx` — Kasir/Antrian/Dapur/Meja + `⋯` overflow, all targets ≥44px, Dapur visibility driven by `kitchenDisplayEnabled`, tabs filtered by `allowedPages` (mirrors `sidebar.tsx`'s own pattern).
- [x] `pos-mode-overflow-menu.tsx` — customer-display trigger (reuses `useCustomerDisplaySettings`), clock in/out (`ClockInOutDialog`), link to `/pos/schedule`.
- [x] `pos-shell.tsx`/`pos-header.tsx` trimmed — `PosHeader` is now just the mobile cart-trigger button (12px bar, `md:hidden`); store name/online/staff/printer/customer-display moved to the shell-level status bar + overflow menu.

## Stage 3 — Staff PIN gate consolidation — ✅ done

- [x] `PosStaffGate` lifted to `(pos-mode)/layout.tsx` (wraps the whole shell, not just `/pos`'s own children) — closes the gap where `/pos/orders`, `/pos/kds`, `/tables` previously had no PIN re-verify.
- [x] Removed the redundant `<PosStaffGate>` wrapper and `bypassStaffGate` prop from `pos-shell.tsx`/`PosShellProps` — layout owns it exclusively now.
- [x] Fixed `pos-staff-gate.tsx`'s two bare `min-h-[calc((100vh-200px)/var(--app-zoom,1))]` heights → `calc(100dvh/var(--app-zoom,1))`.

## Stage 4 — Schedule split (light "my shift" view) — ✅ done

- [x] `(pos-mode)/pos/schedule/page.tsx` rendering `MyScheduleList` (reused, not rebuilt). Sources `staffMemberId` server-side via the existing `getActiveStaffSession()` helper.
- [x] `ClockInOutDialog` reused unchanged, opened from the overflow menu.
- [x] `ROLE_DEFAULT_PAGES.CASHIER`/`.KITCHEN`/`.MANAGER`: `/pos/schedule` wired in.
- [x] `ALL_STAFF_PAGES` fix: `/pos/schedule` unioned in via `posModeNavItems` (confirmed real gap, not speculative).

## Stage 5 — Upgrade-prompt banner in POS Mode — ✅ done

- [x] `discounts: "OPERATIONS"` added to `FEATURE_MIN_PLAN` (confirmed: zero plan gating existed on discounts before this).
- [x] `pos-mode-upgrade-banner.tsx` — dismissible strip, every tap target h-11 (44px). `PosModeUpgradeProvider` (context/state) + `PosModeUpgradeBanner` (presentational).
- [x] `usePosModeUpgradeGate().requireFeature(minPlan, label)` mirrors `useUpgradeGate`'s plan logic with banner presentation instead of a blocking modal.
- [x] `pos-cart.tsx`'s discount popover gated in `openDiscountPopover`.
- [x] Banner CTA routes through the existing `upgradeHrefFor(minPlan)`.

## Stage 6 — Back Office nav trim (data only, not mechanism) — ✅ done, 5 regressions found+fixed

- [x] Removed `/pos`, `/pos/orders`, `/pos/kds`, `/tables` from `dashboardNavigation`'s "Point of Sale" section. Deleted the dead `POS_CASHIER_PATH` special case in `page-shell.tsx`.

**Regressions found by writing tests instead of re-reading code, all fixed**: `ALL_STAFF_PAGES`/`allowedPagesSchema` would reject `/pos`/`/pos/orders`/`/pos/kds`/`/tables` in a hand-edited staffer permission set; `PageAccessChecklist` would drop their checkboxes entirely; `AccountAccessDialog` would render an empty page list for Cashier/Kitchen; `FeedbackDialog`'s page picker would lose them; **`/go/*` PWA launcher would break the manifest's own installed `/go/pos`/`/go/pos/orders` shortcuts** (the most user-visible one). Fixed via `posModeNavItems` + `getAllAppNavItems()`, with every "all pages" consumer switched to the latter.

## Stage 7 — Touch-target and constraints audit — ✅ done

- [x] Converted bare `vh` → `dvh`: `tables-manager.tsx`, `pos-order-queue.tsx` empty states.
- [x] ≥40-44px pass: `pos-checkout-dialog.tsx`, `pos-hold-dialog.tsx`, `refund-dialog.tsx`, `pos-printer-menu.tsx`, `send-receipt-whatsapp.tsx` all bumped from the Button default's 32-36px.
- [x] New Stage 2/5 components built to ≥44px from the start. Zero `hover:`-only controls in `src/features/pos-mode/`.

## Stage 8 — Verification — ⚠️ partial

- [x] Role matrix, kitchen-display toggle, upgrade banner gating, schedule-split permission regression — all covered by passing component/unit tests (`pos-mode-tab-bar.test.tsx`, `pos-mode-upgrade-banner.test.tsx`, `staff-permissions.config.test.ts`).
- [ ] **Real-device/responsive pass, iPad landscape** — not possible in this environment. Needs a human pass before merge.
- [ ] **`loading.tsx`/`error.tsx` regression check under real throttled network** — not exercised live.
- [x] Pushed to `origin/epidom-revamp`, Vercel preview confirmed `READY`.
- [x] `tsc --noEmit` clean (1 pre-existing, unrelated error). `next build` clean, zero route collisions.

**What's left before Phase 1 can merge to `main`**: a real click-through on an iPad or iPad-sized viewport (PIN login, all 4 tabs, checkout, discount wall, overflow menu, clock in/out); confirm the `/go/pos` PWA shortcut fix launches from an installed home-screen icon; confirm the Staff dialog's POS Mode permission checkboxes work live.

---

# Phase 2 — Back Office

Built the brief (`docs/back-office-revamp.md`) the same way Phase 1's spec was built: a deep audit of every current Back Office page, live fetches of Moka/Square/Toast/sunday's *management*-side products, and a pass through STRATEGY.md/FEATURES.md/roadmap.md/AGENTS.md. Conclusion: the left-rail/drawer shell itself doesn't need replacing (all four competitors run equally dense management shells) — what needed fixing was IA debt Phase 1 left behind, plus a pre-existing completeness gap.

**Standout finding**: `/owner` (the flagship Enterprise multi-outlet rollup) lived entirely outside the shell being redesigned — no nav entry, no `PageShell` chrome, unreachable by any staff persona, discoverable only via one button buried in Finance, and not delivering the drill-down its own roadmap promised. The i18n key `nav.owner` existed, unused, in all three locales — evidence this was always the intended next step.

## Stage 1 — Nav config foundation — ✅ done

- [x] `dashboardNavigation` regrouped by job: General (Dashboard, Storefront), Operations (unchanged), Reports (Finance, Owner — renamed from "Enterprise"), Account (new — Profile, Billing, Custom Development). Net still 13 items, no single-item section, no grab-bag.
- [x] `lockedHintKey` added to `NavItem`; all 9 gated items given benefit-framed copy (e.g. Finance: "See P&L and margin by channel") in en/id/fr — matches `STRATEGY.md`'s written upsell philosophy ("explain the event... rather than the feature"), previously violated by a generic `"Upgrade to {plan}"` shown only as an invisible-on-mobile hover title.
- [x] `grantableOnlyNavItems` added (just `/menu`) — grantable, deliberately absent from the rail, same pattern as `posModeNavItems`.
- [x] `requireStaffPageAccess` broadened to accept `string | string[]` (verified backward-compatible against all prior call sites).
- [x] `page-access-checklist.tsx` — new "Other" section sourced from `grantableOnlyNavItems` so an owner can still grant/revoke `/menu` specifically.

## Stage 2 — `/owner` into the shell — ✅ done

- [x] New `(dashboard)/owner/{layout.tsx,page.tsx}`, gated exactly like `finance/` (`requirePlan(storeId, "ENTERPRISE")` + `requireStaffPageAccess`).
- [x] `OwnerDashboardClient` frame trimmed — dropped the hand-rolled `p-4 sm:p-6 lg:p-8` wrapper now that `PageShell` supplies its own padding; removed the now-dead client-side 403 banner (the layout gates server-side now, matching `FinanceClient`'s own pattern, which has no such branch either).
- [x] Per-row drill-down link added to the store table (→ that store's `/finance`, ≥44px tap target) — closes `roadmap.md`'s named, previously-unmet acceptance criterion.
- [x] Old bare `/owner` reduced to `redirect("/go/owner")` — verified `/go/*`'s `LAUNCHABLE_SECTIONS` (derived from `getAllAppNavItems()`) resolves it with zero other changes.
- [x] `finance-client.tsx`'s "All Outlets" button retargeted to `/store/${storeId}/owner`.

## Stage 3 — `/menu` ↔ `/storefront` consolidation — ✅ done

- [x] **Confirmed sharper than planned**: `/menu` required POS tier; Storefront's Menu tab had *no* plan gate at all (verified directly) — a FREE-tier user's only real path to publish a menu was already Storefront's tab, matching `STRATEGY.md`'s named activation metric.
- [x] `storefront-editor-client.tsx` — `?tab=` URL sync added (mirrors `finance-client.tsx`'s own `useSearchParams`/`useRouter` idiom).
- [x] `menu/page.tsx` reduced to a redirect (`/storefront?tab=menu`), plan-gate removed (the destination isn't gated either — a FREE-tier bookmark must still resolve). `menu/layout.tsx` deleted.
- [x] Menu-only-staff detection via `usePosSession()` — a persona granted `/menu` but not `/storefront` still sees bare `MenuManager`, no tabs, identical to the old page's behavior. `requireStaffPageAccess(storeId, ["/menu", "/storefront"])` — either grant works.
- [x] Cleaned up now-stale references: `offline-status.ts`'s `menuEditor` entry removed (redirect-only route needs no offline priming), `last-visited.ts`'s resumable-sections set swapped `/menu` for `/owner`.

## Stage 4 — Locked-nav copy — ✅ done

- [x] `sidebar.tsx`'s locked-item rendering now shows `lockedHintKey`'s text as an always-visible second line (not a `title` hover tooltip) — fixes invisibility on the mobile drawer.
- [x] Stale `~18 in view` comment fixed to the correct current count (13).

## Stage 5 — Constraints & touch-target audit — ✅ done

- [x] No new dialog/sheet/drawer introduced this phase — confirmed by diff review, so the `dvh`-vs-`vh` rule doesn't come into play here (and per direct verification, `page-shell.tsx`/`finance-client.tsx` both already use plain `vh` for in-flow content deliberately — that rule targets dialog/sheet/drawer chrome specifically, not this).
- [x] New locked-item second line verified to *increase*, not shrink, row tap height (two text lines + existing padding ≈ 50px+, well above the 40px floor).
- [x] `/owner`'s drill-down link: `size-11` (44px), not hover-gated.
- [x] No money/quantity value re-parsed into a raw float anywhere this phase touched.

## Stage 6 — Verification & docs — ⚠️ partial

- [x] New/updated tests: `require-staff-page-access.test.ts` (9 cases covering the new array-based grant logic), `navigation.config.test.ts` (extended — `/owner` present+gated, `/menu` grantable-only, every gated item has a `lockedHintKey`, no orphaned single-item section), `sidebar.test.tsx` (rewritten for the new section structure), `storefront-editor-client.test.tsx` (new — 6 cases covering `?tab=` sync and the menu-only-staff branch).
- [x] `docs/back-office-revamp.md` committed.
- [x] `tsc --noEmit` clean (same 1 pre-existing, unrelated error). `next build` clean, all routes compile including `/store/[storeId]/owner`, zero collisions.
- [x] `vitest run`: **116 test files / 1286 tests, all passing** (10 new this phase specifically).
- [ ] **Manual pass across FREE/POS/OPERATIONS/ENTERPRISE test accounts on the Vercel preview** — not done in this environment (no browser automation).
- [ ] **Confirm `/owner` reachability + drill-down, `/menu`'s redirect, and the new locked-hint copy render correctly live** — same honest gap as Phase 1.

**What's left before Phase 2 can merge to `main`**: the same class of human pass Phase 1 needs — click through all 4 plan tiers on the Vercel preview, confirm `/owner`'s drill-down link, confirm a menu-only staff PIN session sees the right (tab-less) view, confirm the id/fr locked-hint translations read naturally (drafted, not reviewed).

---

## Phase 5+ — Next Candidates (pre-v3 backlog, still valid, not scheduled)

- **Google Reviews follow-ups** (v2.91.0 shipped the manual flow: paste a Business Profile review link or Place ID, Storefront → Google Reviews tab). Not built, on purpose: (1) **"Find my business" auto-detect** via the Places API (Text Search on store name + address → pick a result → Place ID). Needs a Google Cloud key with billing enabled — an operator task, and no key exists today (only the Google OAuth client). Build it behind `GOOGLE_PLACES_API_KEY`, degrading to the manual paste when unset. (2) **Review QR on POS receipts** (ESC/POS) — wait for the receipt/printer rework to land. (3) Reading reviews back needs the Business Profile API (OAuth + Google's access approval); out of scope. Google's policy forbids review gating and incentives — never build a "rate 1–5, only send 4–5★ to Google" flow.

- **E2E tests (Playwright)**: 5 critical journeys — sign-up → publish storefront, place online order, open shift → POS sale → close shift, finance report export, multi-outlet owner drill-down. **Both phases above are exactly the kind of work that would have caught what manual verification can't reach here.**
- **Custom domains**: map a merchant's own domain to their `@slug` storefront.
- **Stripe Connect**: 80/20 payment facilitation. Requires legal review for BI/OJK compliance before shipping.
- **Per-outlet manager permissions**: ENTERPRISE stores with multi-outlet need scoped access.
- **Aggregator v2 (official API)**: GoFood/GrabFood partner API. 6–12 month relationship-building track.
- **Cloudflare R2 migration**: swap Vercel Blob for R2 + Cloudflare Images.
- **Singapore DB region**: migrate Postgres when p95 latency from Jakarta exceeds 200ms.
- **`fr.ts` locale gap**: ~587 lines behind `en.ts` (confirmed again this session — several `nav.*` keys are missing entirely, e.g. `production`, `menu`, `customDevelopment`, `billing`, `schedule`), notably the entire `/schedule` page and the POS resume/hold/refund flow — full parity backfill not yet scheduled.
- **`finance-client.tsx` decomposition** (Phase 2's P1 finding): 2,850 lines, single largest component in the codebase. Split by tab into separate files — zero user-visible change, own PR.
